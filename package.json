{
  "name": "cloudflare-workers-openapi",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev --env=production",
    "cf-typegen": "wrangler types",
    "db:setup": "wrangler d1 migrations apply reports-db --local && wrangler d1 migrations apply reports-db",
    "db:local": "wrangler d1 migrations apply reports-db --local",
    "db:prod": "wrangler d1 migrations apply reports-db"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15",
    "chanfana": "^2.8.2",
    "crypto": "^1.0.1",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.9.9",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "22.13.0",
    "@types/service-worker-mock": "^2.0.4",
    "wrangler": "^4.40.2"
  }
}
